# Application name for Spring Boot application
spring.application.name=API

# Port number where the Spring Boot application will run
server.port=8080

# Database connection configuration
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=PackShop;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=1
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA and Hibernate configuration
spring.jpa.properties.hibernate.id.new_generator_mappings=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

# Logging configuration for Hibernate
logging.level.org.hibernate.SQL=FALSE
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
